package com.Ada.SFCAuthenticator.dto;

/**
 * Data Transfer Object (DTO) representing authentication credentials (username and password).
 */
public record AuthenticationDTO(String username, String password) {
}