import axios from "axios";

const API_ENDPOINT = "http://localhost:8080/auth/login";

export const login = async (username, password) => {
  return axios.post(API_ENDPOINT, { username, password });
};
